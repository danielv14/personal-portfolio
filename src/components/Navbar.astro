---
const currentPath = Astro.url.pathname;
const isActive = (path: string) => currentPath === path || currentPath.startsWith(path + '/');
---

<nav class="sticky top-0 z-50 bg-background/80 backdrop-blur-sm border-b border-surface">
  <div class="max-w-3xl mx-auto px-4 py-4 flex items-center justify-between">
    <a
      href="/"
      class="text-lg font-semibold hover:opacity-80 transition-opacity flex items-center"
    >
      <span class="bg-gradient-to-r from-primary to-emerald-400 bg-clip-text text-transparent">[daniel-vernberg]</span><span class="text-text">_</span>
    </a>
    <a
      href="/blogg"
      class:list={[
        "text-lg font-medium transition-colors hover:text-text",
        isActive("/blogg") ? "text-text" : "text-text-muted"
      ]}
    >
      /inlägg
    </a>
  </div>
</nav>
